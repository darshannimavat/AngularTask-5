import { TotalDaysPipe } from './total-days.pipe';

describe('TotalDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
